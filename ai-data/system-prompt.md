# VinAI system prompt (reference)

The live system prompt is `systemPrompt()` in `site-chat/src/bot.ts`:
https://github.com/theVinesh/site-chat/blob/master/src/bot.ts

This file previously held a separate hiring-assistant prompt for a deleted
AI Search instance (`gentle-hall-6ec1`). That instance and its prompt are
gone; bot.ts is now the single source of truth. Update the prompt there,
not here.

Last reconciled: 2026-08-25
